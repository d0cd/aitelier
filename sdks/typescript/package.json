{
  "name": "aitelier",
  "version": "0.1.0",
  "description": "TypeScript SDK for aitelier — an OpenAI-compatible gateway and control plane for LLM and coding-agent calls",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "peerDependencies": {
    "openai": "^4.0.0 || ^5.0.0"
  },
  "peerDependenciesMeta": {
    "openai": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "openai": "^5.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
