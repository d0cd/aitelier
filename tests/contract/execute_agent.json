{
  "description": "Execute a simple agent task",
  "request": {
    "name": "test_agent",
    "kind": "agent",
    "prompt": "List files",
    "workspace": "/tmp/test-workspace",
    "workspace_mode": "copy"
  },
  "expected_response": {
    "required_fields": ["kind", "provider", "text", "duration_s", "status", "run_id"],
    "kind": "agent",
    "status": "ok"
  }
}
