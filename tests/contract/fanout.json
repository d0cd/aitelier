{
  "description": "Fan out across multiple providers",
  "request": {
    "task": {
      "name": "test_fanout",
      "kind": "llm",
      "prompt": "Test prompt"
    },
    "providers": ["provider-a", "provider-b"],
    "max_concurrent": 2
  },
  "expected_response": {
    "type": "array",
    "min_length": 2,
    "each": {
      "required_fields": ["kind", "provider", "text", "duration_s", "status", "run_id"]
    }
  }
}
